@import 'nib'




responsa = {
  gutter: 20px,
  max-width: 1024px
}




container(width)
	margin 0 auto
	max-width unit(width, 'px')
	
	&:after
		visibility: hidden;
		display: block;
		font-size: 0;
		content: " ";
		clear: both;
		height: 0;
		
column(column_width, gutter = responsa.gutter, max-width = responsa.max-width, col = false)
	t = calculate(column_width, gutter, max-width)
	tt = unit(t, '%')
	
	float left
	max-width tt
	
	if (col == false)
		margin-right responsa_margin(gutter, max-width)
	else
		margin-right 0
	
calculate(val, gutter, total)
	t = ((val - gutter) * 100) / total
	return t
	
responsa_margin(gutter, total)
	g = (gutter * 100) / total
	return unit(g, '%')
	



global-reset()

body
	
	.container
		container(1024px)
	
	#header
		background-color #000
		color #fff
		height 60px
		line-height 60px
		
		.container
			background: rgba(0, 0, 0, 0.2);
		
	#main-wrapper
		background-color #eee
		margin 20px 0
		
		.headline
			
			.call
				font-size 30px
				padding 20px 0				
				text-align center
				
		.description
			margin 20px 0
			
			.title,
			.subtitle
				text-align center
				
			.steps
				margin 20px 0
		
		.example
			margin 20px 0
			
			.container
				
				.title,
				.subtitle
					text-align center
				
				.layout
					margin 20px 0

					.col-left
						column(200px)
						
					.col-right
						column(824px, col: 'last')
				
				.code
					clear both
					margin 20px 0
					
					.styl
						column(492px)
					
					.css
						column(512px, col: 'last')
	
	#footer
		background-color #ececec
		clear both
		height 30px
		line-height 30px